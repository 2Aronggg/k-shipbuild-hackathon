# 기획안 대비 변경 사항

WeldScan AI 관리자용 대시보드를 구현하면서 기획안과 달라진 지점, 그 이유, 그리고 구현 과정에서 확인된 데이터 한계를 기록함.

기준 문서: `Scan AI — 비파괴 검사 보조 AI` 기획안 03절 「관리자용 대시보드」

---

## 요약

| # | 기획안 항목 | 상태 | 비고 |
|---|---|---|---|
| ① | 판정-**과거사례** 일치율 추이 | **축 교체** | 판정-**규정** 일치율로 변경 |
| ② | 부위별 이상 징후 경보 | 구현 | 전반기 대비 후반기 급락 탐지 |
| ③ | 시니어 부재 시 지식 공백 경보 | **미구현** | 근무 데이터 부재 |
| ④ | 신입·외국인 판정 소요시간 변화 | 부분 구현 | 연차 축만. 국적 구분 없음 |
| ⑤ | 매칭 실패 케이스 목록 | 구현 | 대상이 사례 → 규정 조항 |
| ⑥ | 에스컬레이션 대기 리스트 | 구현 | 사유 우선순위 정렬 |

기획안에 없었으나 추가된 것: 규정 마진 축, 검사자 판정 눈금, AI 모델 가용 범위 공시, 조항 커버리지, 허용 한계 계산기.

---

## 변경 1 · 대시보드의 기준 축 교체

### 무엇이 바뀌었나

기획안은 관리자 대시보드의 첫 지표를 **"판정-과거사례 일치율"**로 규정함. 구현은 **"판정-규정 일치율"**을 축으로 삼음.

```
기획안   현재 판정  vs  과거 유사사례의 판정      → 일치율
구현     현재 판정  vs  KR 규정표가 계산한 판정   → 일치율
```

### 왜 바꿨나

**과거사례 기준은 기준이 고정되지 않음.** 과거 판정이 옳았다는 보장이 없으므로, 관대한 관행이 사례로 축적되면 시스템이 그 관행에 수렴하는 순환이 발생함. 기준 자체가 시간에 따라 표류함.

**규정 기준은 고정값임.** 한국선급 강선규칙 제2편 적용지침 부록 2-7의 합격기준은 두께와 결함 치수만으로 결정되며 사람 의견이 개입하지 않음. 이 고정된 기준 위에 사람 판정을 겹치면 판정 산포가 직접 측정됨.

**기획안 02절 차별점과의 일관성.** 기획안은 이미 *"왜 불합격인지 선급 규정 조항을 인용하며 근거를 설명"*을 핵심 차별점으로 제시함. 기획안 내부에 규정 기반과 사례 기반 두 패러다임이 혼재했으며, 구현은 규정 기반으로 통일함.

### 부수 효과

기획안의 사례 기반 설계와 연결된 항목들이 함께 변경됨.

- 작업자용 화면의 "유사사례 5건 카드" → 구현 범위 밖
- 관리자용 ⑤ "라이브러리에 없는 신규 패턴" → 규정 조항 검색 실패로 대체

### 사례 검색 검토 결과

형상 특징(결함 개수·최대 긴지름·긴지름 합·둥글기·비드 내 산포·위치) 6차원 벡터로 최근접 검색 프로토타입을 구성해 검증함. 라벨 2,500건 기준, 이웃 5건의 결함 종류가 질의와 일치하는 비율 **85.2%** (무작위 기대치 65.3%) — 검색 자체는 성립.

다만 **사례 일치율은 산출 불가.** AI Hub 라벨에는 결함 종류와 폴리곤만 있고 *"그때 검사자가 어떻게 판정했는가"*가 없음. 사례를 찾아줄 수는 있으나 그 사례의 사람 판정을 붙일 수 없으므로, 기획안 ①이 요구한 일치율은 정의되지 않음.

---

## 변경 2 · 탐지 대상 결함 종류

### 무엇이 바뀌었나

FigJam 파이프라인 다이어그램은 **균열 탐지 모델**(학습 데이터 2,054장)로 표기되어 있으나, 실제 학습된 모델은 **기공 단일 클래스**임.

```yaml
# weldscan_porosity — dataset.yaml
nc: 1
names: {0: porosity}
```

### 왜 바꿨나

학습 데이터 규모 차이가 결정적임.

| 결함 | 이미지 | 결함 개수 |
|---|---|---|
| 기공 | 24,345장 | 88,438개 |
| 균열 | 2,054장 | 4,449개 |

기공이 약 12배 많음.

### 파급 — 규정 처리 경로가 달라짐

| | 균열 | 기공 |
|---|---|---|
| 규정 분류 | 제3종 | 제1종 |
| 판정 방식 | 크기 무관 전건 불합격 | 긴지름으로 점수 산정 후 등급 |
| 모델 요구 성능 | 존재 여부만 | **치수 정확도 필수** |

균열 모델이었다면 마스크 품질이 낮아도 무방했으나, 기공으로 전환하면서 개별 결함 윤곽 정확도가 필수 조건이 됨. 현재 모델 성능은 다음과 같음.

```
이미지 단위   검출률 0.8296 · 정확도 0.9700 · 전체 0.9022
결함 단위     Mask mAP50 0.363 · mAP50-95 0.118
```

대시보드가 요구하는 것은 후자이며, 현 수준으로는 *"기공이 있다"*는 신뢰 가능하나 *"그 기공이 6.2 mm다"*는 신뢰 불가.

**조치 필요**: FigJam 다이어그램의 "균열 탐지 모델" → "기공 탐지 모델" 수정.

---

## 변경 3 · 결함 치수 정의

### 무엇이 바뀌었나

검출기 문서는 룰 엔진 입력으로 `equivalent_diameter_mm`(면적 기준 등가원 지름) 사용을 안내하나, 구현은 폴리곤에서 **긴지름**을 직접 계산해 사용함.

```python
def major_diameter_px(polygon_xy):
    p = np.asarray(polygon_xy, float)
    d2 = ((p[:, None, :] - p[None, :, :]) ** 2).sum(-1)
    return float(np.sqrt(d2.max()))
```

### 왜 바꿨나

KR 부록 2-7 표14가 요구하는 값이 **결함의 긴지름**임. 등가지름은 같은 면적의 원으로 환산한 값이므로 결함이 길쭉할수록 작게 산출됨.

RT 일반강재 기공 라벨 10,945개 실측 결과:

```
긴지름 중앙값      54.4 px
등가지름 중앙값     40.5 px
등가 ÷ 긴지름      0.765 (중앙값)
```

두께 20 mm · 배율 0.10 기준으로 마진까지 반영하면:

```
긴지름 기준   불합격률 91.4%
등가지름 기준  불합격률 81.1%
판정 역전     10.3% — 전부 불합격 → 합격 방향
```

역전 방향이 결함을 놓치는 쪽이므로 선급 검사에서 문제가 되는 유형의 오류임.

### 조치

`polygon_xy`가 검출기 출력 스키마 v1.0에 이미 포함되어 있으므로 검출기 수정 없이 연결 계층에서 우회함. 다만 `detector.py`에 `major_diameter_mm` 필드를 추가하는 편이 계약상 명확함.

---

## 변경 4 · 미구현·부분 구현 항목

### ③ 시니어 부재 경보 — 미구현

근무·부재 데이터가 존재하지 않음. 합성 생성기에 일별 `senior_on_duty` 필드를 추가하면 부재일과 근무일의 불일치율·에스컬레이션 적체를 비교하는 형태로 구현 가능.

### ④ 외국인 인력 구분 — 부분 구현

연차 필터로 신입 집단의 판독 소요시간은 확인 가능하나 다음 두 가지가 미흡함.

- 소요시간의 **시간에 따른 변화 곡선**이 없고 기간 평균 한 점만 표시됨
- **국적 구분 필드가 없음**. 검사자 명단에 외국인 이름 3명이 포함되어 있으나 이름으로만 암시됨

### ⑤ 매칭 실패 — 대상 변경

기획안은 *사례 라이브러리*에 없는 신규 패턴을 의미하나, 구현은 *규정 조항* 검색 실패를 표시함. 구조는 동일하되 검색 대상이 다름.

---

## 확인된 데이터 한계

구현 과정에서 확인된 사항으로, 기획안 작성 시점에는 알 수 없었던 내용임.

### 픽셀-실치수 환산값 복원 불가

규정은 밀리미터 단위이고 라벨은 픽셀 단위이나, 두 단위를 잇는 값이 데이터셋에 없음.

확인 경로:

- 데이터설명서 — 촬영 조건 항목 없음
- 어노테이션 포맷 15개 필드 — 픽셀값만 존재
- AI Hub 데이터셋 페이지 — "1280×720 픽셀 이상"이 전부
- 공식 배포 AI 모델 문서 5종 — 없음. `--img 800` 리사이즈로 탐지만 수행
- **구축활용가이드 — 원인 확인**

구축활용가이드 RT 데이터 정제 절차에 다음과 같이 기술됨.

> RT 필름 스캐너 활용 RT 필름 → JPG 변경 작업 진행
> 결함 부위를 정확히 식별한 후 **해당 부위 중심으로 이미지 확대**
> 확대 과정에서 이미지의 해상도는 최소 1280×720 이상 확인

수집 기준란에는 `객체의 크기 비율: 제한 없음`, `이미지 비율 및 해상도: 1280×720 이상(비율 및 가로세로 무관)`으로 명시됨.

즉 원천데이터는 필름 스캔본이 아니라 **결함 부위를 임의 배율로 확대한 이미지**이며, 배율이 기록되지 않음. 실측 결과 원본 파노라마의 세로 해상도가 **682종**으로 분산됨. 단일 값은 물론 이미지별 값도 복원 불가.

**대응**: 값을 고정하지 않고 슬라이더로 노출해 민감도를 정량화함.

```
두께 20 → 50 mm      RT 불합격률 51.6% → 43.4%
배율 0.10 → 0.25     RT 불합격률 51.6% → 56.0%
```

모재두께보다 필름 배율이 등급을 더 크게 흔듦. 현장 도입 시에는 RT 장비 사양에서 확보되며 확대 처리가 없으므로 이 한계는 소멸함.

### KS B 0845 원문 미확보

합격선(마진 1.0)은 KR 규정 원문이므로 확정값이나, 1~4급을 구분하는 경계는 원문 미확보로 선급 허용 한계의 1/2·2배로 잠정 설정함. 원문 확보 시 `grade_of()` 함수만 교체하면 됨.

### VT 언더컷 판정 제외

외관검사 언더컷은 라벨에 폴리곤 길이만 있고 표12가 요구하는 **깊이**가 없어 판정 불가. 표본 12,067행 중 **1,098행(9.1%)**을 판정에서 제외하며, 이 사실을 화면에 명시함.

---

## 기획안에 없던 추가 구현

| 패널 | 목적 |
|---|---|
| 규정 마진 축 | 사람과 규정이 갈리는 지점을 마진 한 축 위에 배치 |
| 검사자 판정 눈금 | 검사자별 불합격 전환 마진을 역산. 기획안 01절 "불일치율 15~20%"의 측정 장치 |
| AI 모델 가용 범위 | 기공 단일 클래스, 배율 미확정 등 전제 조건 공시 |
| 조항 커버리지 | 244청크 중 판정 근거로 연결되지 않은 영역 표시 |
| 허용 한계 계산기 | 두께 입력 시 규정 허용치를 즉시 조회. 임의 점수가 아님을 증명 |
| 실측·합성 배지 | 각 구획의 데이터 출처를 화면에 명시 |

기획안 01절은 판정 불일치율 15~20%를 **문제**로 서술하되 측정 방법은 제시하지 않음. 검사자 판정 눈금 패널이 그 측정을 담당함.

---

## 남은 작업

| 항목 | 필요 조건 |
|---|---|
| 학습 가중치 연결 | `best.pt` 확보 (코드·추론 경로 완성 상태) |
| 필름 배율 확정 | 현장 RT 장비 사양 |
| 등급 경계 확정 | KS B 0845 원문 |
| 합성 패널 실측 전환 | 참여 조선소 판정 이력 |
| ③ 시니어 부재 경보 | 생성기에 근무 필드 추가 |
| FigJam 다이어그램 수정 | "균열 탐지 모델" → "기공 탐지 모델" |
